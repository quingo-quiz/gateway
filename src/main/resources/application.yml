spring:
  application:
    name: gateway

  cloud:
    gateway:
      server:
        webflux:
          routes:
            - id: auth-route
              uri: http://${AUTH_SERVICE_HOST}
              predicates:
                - Path=/auth/**

            - id: quizzes-route
              uri: http://${MAIN_SERVICE_HOST}
              predicates:
                - Path=/quizzes/**

            - id: users-route
              uri: http://${MAIN_SERVICE_HOST}
              predicates:
                - Path=/users/**

logging:
  level:
    org.springframework.cloud.gateway: DEBUG